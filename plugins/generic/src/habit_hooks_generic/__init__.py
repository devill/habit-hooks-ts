"""The generic Habit Hooks plugin: language-agnostic sensors and guides.

This package ships only data — its ``config.toml``, ``sensors/``, ``guides/``,
``.jscpd.json`` and helper scripts — discovered by the core through the
``habit_hooks.plugins`` entry point.
"""
